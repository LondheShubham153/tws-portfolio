# Demo project for github actions
  Create a S3 bucket on aws and generate access keys for the same.
  Make sure bucket has static-website enable and generate bucket policy for publicreadgetobject.
  Add you secrets in repo settings 
  Create yaml file for github actions
